namespace FichaCadastro.Mappers
{
    public class ParoquiaMapping
    {
        public ParoquiaMapping()
        {
            //Table("T_PAROQUIA");
            //Id(x => x.Id).Column("ID").GenerateBy.Assigned();

            //Map(x => x.).Column("DC_PAROQUIA");
            //Map(x => x.).Column("END_DC_PAROQUIA");
            //Map(x => x.).Column("END_BAIRRO_PAROQUIA");
            //Map(x => x.).Column("PAROCO");
        }
    }
}
